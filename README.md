# AutoSpawn Pokeone
### como o bot funciona
- envio de mensagens

```python
		channel = client.get_channel(int(chat))
        await channel.trigger_typing()
        await channel.send(f'{prefixo}s')

```
